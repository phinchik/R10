import React from "react";
import { View } from "react-native";
import Map from "./Map";

const MapContainer = () => {
  return (
    <View>
      <Map />
    </View>
  );
};

export default MapContainer;
